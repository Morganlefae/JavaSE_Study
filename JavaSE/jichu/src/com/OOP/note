super
    1. super 调用了父类的无参构造，必须在第一行。
    2. super 必须出现在子类的构造方法中。
    3. super 和 this 不能同时调用构造方法。
this
    1. 没有继承也能使用
    2. 调用本类的构造

重写
1需要有继承关系，子类重写父类
2方法名，参数列表必须相同
3修饰符范围可以扩大但不能缩小
4抛出的异常的范围可以缩小不能扩大
重写，子类的方法和父类必须一致

为什么需要重写
    1.父类的功能子类不一定需要
    Alt + enter: override
多态
    多态是静态方法的多态， 属性没有多态
    父类子类有关系， 类型转换异常： ClassCastException
    父类引用指向子类对象
    不能重写：
    1 static 属于类，不属于实例
    2 final 常量
    3 private
